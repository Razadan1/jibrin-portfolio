`# Build Fix TODO

1. [x] Add `@types/node` to `devDependencies` in `package.json`
2. [x] Add `"types": ["node"]` to `tsconfig.node.json`
3. [x] Replace `__dirname` with ES module-compatible code in `vite.config.ts`
4. [x] Run `npm install` to install the new dependency
5. [x] Run `npm run build` to verify the fix


